info:
  name: Rick eliminado
  type: http

http:
  method: DELETE
  url: http://localhost:8080/api/personagem/Rick
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
