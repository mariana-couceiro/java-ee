info:
  name: personagem Rick
  type: http
  seq: 1

http:
  method: POST
  url: http://localhost:8080/api/personagem
  body:
    type: json
    data: |-
      {
        "nome": "Rick",
        "especie": "Humano",
        "comidaFavorita": "Pizza"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
