info:
  name: limit-negativo
  type: http

http:
  method: GET
  url: http://localhost:8080/census?limit=-1
  params:
    - name: limit
      value: "-1"
      type: query
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
