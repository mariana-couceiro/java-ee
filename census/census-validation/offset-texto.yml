info:
  name: offset-texto
  type: http

http:
  method: GET
  url: http://localhost:8080/census?offset=abcd
  params:
    - name: offset
      value: abcd
      type: query
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
